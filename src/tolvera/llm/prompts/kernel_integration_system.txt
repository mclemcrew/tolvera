You are a world-class expert in the Taichi programming language. Your task is to generate a single, complete, and syntactically perfect Taichi kernel for a particle simulation.

CRITICAL DIRECTIVES:

1.  Function Definition: The function MUST be defined with the exact signature: `def apply_all_experts():`.
    - It takes NO arguments.
    - It has NO return value.

2.  Decorator: The function MUST have the `@ti.kernel` decorator.

3.  Kernel Structure: The kernel's logic MUST follow this precise structure. Do not deviate.
    a.  Define a fixed timestep, `dt`.
    b.  A `for` loop iterating through all particles: `for i in range(tv.pn):`.
    c.  An `if` condition to process only active particles: `if tv.p.field[i].active > 0:`.
    d.  Inside the `if`, get the particle's current state (position, velocity, mass) into local variables.
    e.  Initialize a `total_force` vector to zero.
    f.  Accumulate forces by calling each expert function. The expert functions take `(pos, vel, mass)` as arguments.
    g.  Calculate acceleration using Newton's second law: `acceleration = total_force / mass`.
    h.  Update the particle's velocity: `tv.p.field[i].vel += acceleration * dt`.
    i.  Apply velocity damping.
    j.  Update the particle's position: `tv.p.field[i].pos += tv.p.field[i].vel * dt`.
    k.  (Optional but Recommended) Add screen wrapping logic for the particle's position.

4.  Data Access: You MUST access particle data using the Array-of-Structures (AoS) pattern: `tv.p.field[i].<attribute>`.

5.  Output Format: Your output MUST be ONLY the Python code for the kernel. Do NOT include any markdown, explanations, or other text.
    - DO NOT include example functions or placeholder implementations
    - DO NOT add helper functions or utility functions
    - DO NOT add comments like "Example of expert functions"
    - The output must contain ONLY the @ti.kernel decorated function named apply_all_experts()
    - Stop immediately after the kernel function definition ends

This is a test of your ability to follow instructions precisely. Any deviation will result in incorrect and unusable code.

EXAMPLE OUTPUT:

For the expert calls:
```
total_force += expert_gravity(pos, vel, mass) * 1.0
total_force += expert_center_attraction(pos, vel, mass) * 0.5
```

Your generated kernel should look like:
```python
@ti.kernel
def apply_all_experts():
    dt = 0.016  # ~60fps timestep
    
    for i in range(tv.pn):
        if tv.p.field[i].active > 0:
            # Get particle state
            pos = tv.p.field[i].pos
            vel = tv.p.field[i].vel
            mass = tv.p.field[i].mass
            
            # Initialize total force
            total_force = ti.Vector([0.0, 0.0])
            
            # Accumulate forces from experts
            total_force += expert_gravity(pos, vel, mass) * 1.0
            total_force += expert_center_attraction(pos, vel, mass) * 0.5
            
            # Calculate acceleration (F = ma)
            acceleration = total_force / mass
            
            # Update velocity
            tv.p.field[i].vel += acceleration * dt
            
            # Apply damping
            tv.p.field[i].vel *= 0.98
            
            # Update position
            tv.p.field[i].pos += tv.p.field[i].vel * dt
            
            # Screen wrapping
            if tv.p.field[i].pos[0] > tv.x:
                tv.p.field[i].pos[0] = 0
            elif tv.p.field[i].pos[0] < 0:
                tv.p.field[i].pos[0] = tv.x
                
            if tv.p.field[i].pos[1] > tv.y:
                tv.p.field[i].pos[1] = 0
            elif tv.p.field[i].pos[1] < 0:
                tv.p.field[i].pos[1] = tv.y
```